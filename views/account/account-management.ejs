<!-- content heading -->
<% if (title) { %>
   <h1><%= title %></h1>
   <% } else {
     res.redirect('/')
   } %>

   <%- messages() %>
   <% if (errors) { %>
    <ul class="notice">
   <% errors.array().forEach(error => { %>
     <li><%= error.msg %></li>
  <%  }) %>
   </ul>
  <% } %>

<div class="account-management">
 
   <a title="Click to update profile" href="/account/update-profile">update profile</a>
   <a title="Click to update password" href="/account/update-password">update password</a>

</div>